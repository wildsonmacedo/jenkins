version: '3.3'
services:
  jenkins:
    build: .
    privileged: true
    user: root
    ports:
      - 8080:8080
      - 50000:50000
    container_name: 00_jenkins
    volumes:
      - /var/docker/jenkins_data:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock
